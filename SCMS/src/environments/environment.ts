import { environment as assetsEnvironment } from '../assets/environments/environment';

/**
 * Re-export the existing assets-based environment so imports can use
 * `src/environments/environment` without changing the original source.
 */
export const environment = assetsEnvironment;

